# CLI 命令精简计划

## 问题

当前 CLI 有 279 个命令，其中 200+ 个（71.7%）是 CRUD 的重复语法糖。

## 解决方案

保留 CRUD 通用命令，**废弃**专用模块中的重复 CRUD 命令。

## 实施方式

### 方式一：添加废弃警告（推荐，向后兼容）

保留所有命令，但添加废弃警告：

```bash
$ ssos-cli accounting account-list
⚠️  DEPRECATED: Use 'ssos-cli crud list accounts' instead
This command will be removed in v2.0.0

# 然后正常执行命令
```

**优点**：
- 向后兼容，不破坏现有脚本
- 给用户迁移时间
- 引导用户使用正确命令

**实施时间**：2-3 小时

### 方式二：立即删除（激进，破坏性）

直接删除重复命令，只保留 CRUD 和专用逻辑。

**缺点**：
- 破坏现有脚本
- 用户体验差
- 无迁移期

**不推荐**

## 推荐实施计划

### Phase 1：添加废弃警告（本周）

1. 在每个重复命令前添加警告
2. 更新文档，推荐使用 CRUD
3. 发布 v1.1.0

### Phase 2：观察期（1-2个月）

1. 监控命令使用情况
2. 收集用户反馈
3. 提供迁移指南

### Phase 3：删除废弃命令（v2.0.0）

1. 删除所有重复命令
2. 只保留 79 个有价值命令
3. 发布 v2.0.0

## 要立即实施吗？

如果用户同意，我可以立即：
1. 添加废弃警告到重复命令
2. 或者直接删除重复命令（需要确认）

请指示下一步行动。
